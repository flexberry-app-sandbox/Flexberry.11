import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('transform:i-i-s-1-управляющий-объект-з-с', 'Unit | Transform | i i s 1 управляющий объект з с', function(hooks) {
  setupTest(hooks);

  // Replace this with your real tests.
  test('it exists', function(assert) {
    let transform = this.owner.lookup('transform:i-i-s-1-управляющий-объект-з-с');
    assert.ok(transform);
  });
});
