import ObjectFromEmber from '@ember/object';
import { ObjectMix as УправляющийОбъектЗСMixin } from
  '../mixins/regenerated/objects/IIS1УправляющийОбъектЗС';

let EmberObject = ObjectFromEmber.extend(УправляющийОбъектЗСMixin, {

});

export default EmberObject;
